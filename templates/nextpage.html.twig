
{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <div class="div100">
        <h1>{{ title }}</h1>
        <p><a href="/" class="btn">< Back</a></p>
    </div>
    <div class="div50 text-center img-container">
        <img src="{{ data.img }}"></img>
    </div>
    <div class="div50">
        <h2>{{ data.name }}</h2>
        <p>{{ data.description }}</p>
    </div>
    <div class="clear"></div>
</div>

<style>
.img-container img{
    max-width:100%;
    max-height: 600px;
}
</style>

{% endblock %}